import { DisplayTimeToPipe } from './display-time-to.pipe';

describe('DisplayTimeToPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayTimeToPipe();
    expect(pipe).toBeTruthy();
  });
});
