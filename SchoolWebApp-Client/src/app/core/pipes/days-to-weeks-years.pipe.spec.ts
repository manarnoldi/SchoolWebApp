import { DaysToWeeksYearsPipe } from './days-to-weeks-years.pipe';

describe('DaysToWeeksYearsPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysToWeeksYearsPipe();
    expect(pipe).toBeTruthy();
  });
});
